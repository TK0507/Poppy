[TASKS 2024/07/30 16:31]
[AUTHOR] 木村　拓耶
[NOTE] このテキストは具体的なタスク分割内容についての日々の記録です

section-1 今日の目標 {

    今回の作業内容は

    > データセットの準備
    > データセットの外部化

    が主な作業内容です。

    ここで、データセットの準備に関して、
    素早く準備できるわけではないので、まずはデータセットの外部化を進めます。
    
    同時並行で、データ収集のために協力者を募り、テキストデータおよび評価値を用意します。
}

section-2 作業内容1 {

    今日の最初の作業内容を記録します。

    作業内容: データセットの外部化

    データセットの外部化は一応完成しました。

    csv形式による簡易的な読み込みが現在の実装状況です。

    この作業は完了したため、特に理由のない限り外部化形式の変更及び再検討は

    無期限に延期されます。
}

section-3 作業内容2 {

    今日の2回目の作業内容を記録します。

    作業内容: データセットの準備。

    前回はデータセットがハードコーディングされていたため、
    それらのデータを前回の作業内容の形式に従って外部化しました。

    また、新たなデータサンプルを調達するため、
    知人に協力してもらい、1時間分の議論チャットのテキストデータを入手しました。

    ただし、これらは不完全なサンプルです(評価値が存在しないため)

    これらのサンプルの評価方法として、
    協力者にGoogleフォームなどを用いてスコアリングに協力してもらう予定です。

    この作業は長期的なものとなるため、プログラムタスクの終了後も継続的に実施されます。
}

section-4 作業内容3 {

    今回の3回目の作業内容を記録します。

    作業内容: アプリケーションの部分的改善

    いくつかの既存の機能の非機能要件を修正しました。

    1.モデルの読み込みをウェイトキャッシュの保存によって効率化しました
    2.Modelクラスにfit2メソッドを追加し、複数のサンプルを同時に学習するように変更しました
    3.学習エポック数を100回に修正しました。

    1に関しては2回目以降の起動速度が向上します。
    2に関しては非機能要件でもありますが、モデルの予測精度という意味では機能要件でもあります。
    3に関しても、ひきの要件でもありますが2と同様に機能要件でもあります。
}

section-5 まとめ {

    今回の作業内容をまとめます。

    以下の作業が完了しました。

    > データセットの外部化
    > アプリケーションの部分的改善

    以下の作業が継続中です。

    > データセットの準備

    ただし、この作業内容はアプリケーション開発中に継続的に実施されるため、
    優先的事項ではあるものの最優先事項ではありません。

    次回以降の大まかな予定を記録します。

    以下が、次回以降の作業内容の候補です。

    > データセットの準備
    > ユーザーインターフェースの拡張
    > フロントエンドの作成
    > モデルの拡張
    > モデルの改善
    > BERTの置換

    これらを踏まえて、次回以降のアプリケーション開発には
    特定のAPI実装を提供すること、およびモデルの拡張が予定されるかもしれません。

    ただし、現時点での評価ではBERTの置換はコストが高いと判断したため、
    検討対象ではありますが、特に理由のない限り実施は無期限に延期されます。
}